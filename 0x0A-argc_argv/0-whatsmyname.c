#include "main.h"
/**
 * main - the entry point
 * @argc: the counter of arguments
 * @argv: the array of arguments
 * Return: a zero (success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - 1]);
	return (0);
}
