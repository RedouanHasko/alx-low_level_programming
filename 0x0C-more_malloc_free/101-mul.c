#include "main.h"
#include <ctype.h>

/**
 * mul - multiplies two numbers
 * @n1: the 1st number to multiplie.
 * @n2: the sec number.
 * Return: multiplication of n1 and n2
 */
int mul(int n1, int n2)
{
	return (n1 * n2);
}
int main(int argc, char *argv[])
{
	if (argc != 3)
	{
		printf("Error\n");
		return ();
	}
}
